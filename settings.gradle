rootProject.name = 'geerok-backend'

include(
        'core',

        'boot:api-server',

        'support:support-jwt',
        'support:support-api',

        'account:account-rest',
        'account:account-app',
        'account:account-domain',
        'account:account-infra',
)